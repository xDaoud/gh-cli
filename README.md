# GH CLI

A simple TypeScript command-line interface (CLI) to explore GitHub repositories, issues, and repo details.

## Features (planned)

- List repositories of a user: `repos <username>`
- List issues of a repository: `issues <owner>/<repo>`
- Show repository details: `repo <owner>/<repo>`
- Optional GitHub authentication for higher rate limits
